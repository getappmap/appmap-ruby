source "https://rubygems.org"

git_source(:github) {|repo_name| "https://github.com/#{repo_name}" }

gem 'method_source', git: 'https://github.com/symwell/method_source', branch: 'sw/faster_extract_last_comment'

gemspec

gem 'rack', '~> 2'
